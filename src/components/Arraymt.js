import React from 'react'

function Arraymt(props) {
  return (
    <div>
        <h3> {props.d}this is your id {props.c.id} and then this your{props.c.name}after then{props.c.company}</h3>
    </div>
  )
}
export default Arraymt